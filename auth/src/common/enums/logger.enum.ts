export enum ELoggerCtx {
  CACHE = 'Cache',
  EMAIL = 'Email',
  FACEBOOK = 'Facebook',
  GITHUB = 'GitHub',
  GOOGLE = 'Google',
  HTTP = 'HTTP',
  SERVICE = 'Service',
  SQL = 'SQL',
}

export enum ELogLevel {
  DEBUG = 'debug',
  ERROR = 'error',
  INFO = 'info',
  TRACE = 'trace',
  WARN = 'warn',
}

export enum ELogClient {
  CONSOLE = 'console',
}
