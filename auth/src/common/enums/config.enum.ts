export enum EConfigKey {
  APP = 'AppConfig',
  DATABASE = 'DatabaseConfig',
  EMAIL = 'EmailConfig',
  JWT = 'JwtConfig',
  LOGGER = 'LoggerConfig',
  PLATFORM = 'PlatformConfig',
  REDIS = 'RedisConfig',
}
